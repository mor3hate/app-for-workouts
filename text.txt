переменные окружения-данные о настройке сервера
dotenv позволяет обращаться к ним для настройки сервера на ноде(компонент)
morgan(отладка) логирование запросов в одном месте
post - добавить 
get - получить 
rest api - клиент-серверное взаимодействие
endpoint (часть url, которая содержит например имя вызываемого метода).
CRUD - CREATE: post READ: get UPDATE - update DELETE - delete
jwt - токен, хэдер, данные, кодировка(когда есть ключ)
auth -> access, refresh, +user -> localStorage, cookies(лучше, безопаснее)

token = req.header.authorization.split(' ')[1] - разделяет строку на массив, 1 элемент - токен, 0 - Bearer

select позволяет выбирать, - пароль, не нужен пароль
next - идти дальше
populate() - раскрытие ObjectId